Работа:
I режим
1. Получаем адрес файла с  настройками -  текущей версией, именем парсера для папки, имени парсера для поиска файлов, адрес откуда качать обновления (папку с подпапками или адрес в сети с подпапками), имя ini файла для распаковки,
имя файла запускаемой программы.
2. Парсим адрес и смотрим есть ли более новая версия.
3. Выводим в стандартный поток вывода номер новой версии если она есть, 'no' если ее нет, 'error' если не удалось соединиться с сервером.
В error выводим

II режим
1. Получаем имя новой версии или 'auto' и  адрес файла с  настройками -  текущей версией, именем парсера для папки, имени парсера для поиска файлов, адрес откуда качать обновления (папку с подпапками или адрес в сети с подпапками)  имя ini файла длдя распаковки
имя файла запускаемой программы.
2. Парсим адрес и в зависимости от первого параметра  находим адрес откуда качаем все файлы поиском всех объектов с помощью второго парсера в директорию update_src.
3. Находим ini файл
4. Проверяем CRC всех скаченных файлов, при ошибке пробуем перекачать заново эти файлы (3 попытки с паузой 5 секунд).
    4.1 При ошибке выводим сообщение пользователю об ошибке и последнем Exception и выходим
5. по Ini файлу копируем все файлы /папки из файлов указаных в INI  как места назначения во временную директорию update_tmp/номер текущей версии
6. Распаковываем все файлы (если у них признак unpack) в соотвествующие подпапки с заменой всех файлов.
    6.1 Проверяем CRC этих файлов. Если все удачно то переписываем в файле настроек имя версии на новое значение, сообщение, что закончено успешно, запустить программу? (если есть строчка в файле с настройками).
    6.2 При ошибке пытаемся скопировать неудачные файлы три раза с паузой 5 секунд, возвращаем все старые файлы назад. При ошибке - Fatal error
    выводим адрес с папкой где находится резервная копия - говорим, не удалось, требуется востановление - предидущая версия находится по адресу: update_tmp/номер текущей версии

распаковка архивов:
1. Получаем адрес архива
2. Получаем адрес назначения
3. Получаем ссылку на интерфейс с CRC
3. Распаковываем
4. возвращаем список FileItem для работы

||| режим
1. Ничего не получаем
2. Открывается меню пользователя
3.

Добавить вывод пользователю списка классов предназначеных для сканирования версии
